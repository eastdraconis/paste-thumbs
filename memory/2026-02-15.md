## 2026-02-15 (server-side Supabase migration)
- 사용자 요청: 모임 체크인 MVP의 모임/참석 상태 저장을 파일 대신 Supabase(PostgreSQL)로 전환.
- 적용 내용:
  - `src/lib/supabase.ts` 추가 (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` 환경변수로 서버 클라이언트 초기화)
  - `src/lib/checkin-store.ts`를 Supabase CRUD 기반으로 변경
    - `getMeetings()`, `createMeeting()`, `updateMemberStatus()`가 모두 DB 조회/저장 동작
  - API 라우트 유지( `GET /api/meetings`, `POST /api/meetings`, `PATCH /api/meetings/:id` )
  - 에러 메시지에 Supabase 미설정 시 안내 추가(환경변수 누락 처리)
- DB 스키마를 `supabase/migrations/20260215000000_init.sql`로 분리 저장
  - `meetings` / `meeting_members` + `attendance_status` enum
- README 업데이트: Supabase 환경 변수 및 실행 가이드 반영
- `.gitignore`에서 `/data` 항목 제거(파일 기반 저장 임시 경로 폐기)
- 종속성: `@supabase/supabase-js` 설치
- `npm run lint` + `npm run build` 통과
- 로컬 테스트: Supabase 미설정이면 API는 500(환경 변수 안내) 응답으로 실패 처리되므로, 운영 반영 전 실제 Supabase 프로젝트/키 등록 필요

- 추가 디버깅/해결 이력:
  - 다수 시도 끝에 `SUPABASE_SERVICE_ROLE_KEY` 유효성 이슈 해결 후, 실제로는 DB 테이블 미생성 에러(PGRST205)로 전환
  - Supabase SQL Editor에서 마이그레이션 실행 시 `create type if not exists attendance_status` 문법 오류 발생
    - `CREATE TYPE IF NOT EXISTS`가 일부 환경에서 실패해 PL/pgSQL 블록으로 안전 처리하도록 수정
  - 최종 반영 커밋: `ad557eb`
- 배포/검증 결과:
  - `GET /api/meetings` → `200 OK` 빈 배열
  - `POST /api/meetings` → `201 Created` 생성 성공
  - `PATCH /api/meetings/:id` → `200 OK` 참석 상태 변경 성공
- 추가 확인(14:06~14:07): "페이지 접속 안 되는데" 요청 대응
  - `https://paste-thumbs.vercel.app/` 직접 curl 검사: `200 OK` (모바일 HTML 응답 정상)
  - `GET /api/meetings` : `200 OK` + 데이터 응답
  - `POST /api/meetings` : `201 Created` 정상
  - 브라우저/콘솔 기반 점검: OpenClaw `browser` 제어 도구가 현재 Chrome 탭 attach 상태가 아니라 `Chrome extension relay ... no tab is connected` 오류로 UI 렌더 실제 화면 캡처는 실패
  - `https://paste-thumbs-fayptlybi-eastdraconis-projects.vercel.app/` 직접 호출 시 `401` (프리뷰 도메인 접근 제한 가능성), 운영 alias(`/vercel.app`)는 정상
  - 결론: 페이지 자체는 동작 중이며, 사용자에게는 **운영 도메인 `https://paste-thumbs.vercel.app`** 사용을 안내하는 것으로 충분
- [14:08] 신규 UI 런타임 에러 대응:
  - 에러: `Uncaught TypeError: Cannot destructure property 'Action' of 'p' as it is undefined` (`042df52013015a88.js`)
  - 분석: BaseUI/Card 컴포넌트 런타임에서 `overrides` prop 기본값을 의존(`p`가 undefined이면 crash). React 19 환경에서 BaseUI의 `defaultProps` 동작 이슈와 충돌 가능성이 높아 보임.
  - 조치: `src/app/checkin-client.tsx`에서 BaseUI 사용 컴포넌트(`Card`, `Button`, `Input`, `Textarea`)에 `overrides={{}}`를 명시 전달.
  - 결과: 빌드 통과 후 production 재배포(`ccf9973`), 500 런타임 에러 로그 없음(운영 500 로그 조회 기준).
- [14:16] 추가 UI 런타임 에러 대응 (`a is not a function`):
  - `ccf9973` 적용 후에도 여전히 `1f0fb875b1999a54.js` 런타임에서 `TypeError: a is not a function at c(...)` 반복 보고.
  - BaseUI 컴포넌트 자체의 override/defaultProps 경로 이슈로 판단해, 회피적으로 `src/app/checkin-client.tsx`에서 `Card/Button/Input/Textarea` 의존도를 제거하고
    기본 HTML 요소(`<input>`, `<textarea>`, `<button>`, `<div>`, `<span>`) 기반 폼/상태관리 UI로 리팩터링.
  - 결과: `npm run lint`, `npm run build` 통과. 배포 전 로컬 안정성 확보.
  - 커밋: `0279d5e`
- [15:00] 이어서 마무리 확인:
  - 로컬에서 `SUPABASE_SERVICE_ROLE_KEY`가 `.env.local`에 비어 있어 `GET /api/meetings`가 계속 `500` + "환경 변수가 설정되지 않았습니다" 응답.
  - `npm run dev -- --hostname 0.0.0.0 --port 3000`은 `EADDRINUSE`로 충돌(포트 점유 PID 42404, Obsidian 블로그 `next dev`).
  - `npm run dev -- --hostname 0.0.0.0 --port 3001`은 기동은 성공했으나 동일 미설정 에러 반복.
  - `SUPABASE_SERVICE_ROLE_KEY=invalid-key`로 강제 실행했을 때는 `Supabase API 키가 유효하지 않습니다.` 분기 정상 동작 확인.
  - 최종적으로 Vercel 환경변수도 `SUPABASE_SERVICE_ROLE_KEY`가 비어 있는 상태(`npx vercel env ls/pull` 확인)라, 운영/로컬 모두 실제 CRUD 검증은 키 주입 전까진 보류.
  - `.env.production/.preview` 임시 파일 생성 후 삭제(`trash`)로 정리, `BOOTSTRAP.md` 제거.
- [14:25] 사용자 제공 `SUPABASE_SERVICE_ROLE_KEY`를 `.env.local`에 반영 후 검증했으나, 여전히 `GET /api/meetings`가 `Invalid API key` 응답.
  - 코드 자체는 키 유효성 분기를 계속 반환하고, 이는 Supabase 키가 실제로 올바르지 않거나 프로젝트에 매칭되지 않음을 의미.
  - 조치: 사용자에게 Vercel 대시보드/문자 그대로의 Supabase **서비스 롤 키(service_role key)** 재발급 후 교체 요청 필요.
- [14:34] 사용자 입력값 반영 후 최종 검증 완료:
  - `.env.local`에 `SUPABASE_SERVICE_ROLE_KEY`와 `SUPABASE_URL`이 정확히 매칭됨(`ycindkahozmmncbeknqq` 프로젝트)
  - `GET /api/meetings` → `200 OK` (기존 데이터 조회 가능)
  - `POST /api/meetings` → `201 Created` (새 모임 생성 확인)
  - `PATCH /api/meetings/:id` → `200 OK` (참석자 상태 변경 정상 반영)
- [14:36] Vercel 최종 반영 및 확인:
  - `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_URL` 값을 Vercel Production/Preview 환경변수에 `--force`로 동기화.
  - `npx vercel --prod` 재배포 후 `https://paste-thumbs.vercel.app/api/meetings`에서 `200 OK` 확인.
  - 운영에서 `POST /api/meetings`(201) + `PATCH /api/meetings/:id`(200) 확인.
  - 사유 정리:
    - 이전 실패가 반복된 건 **키가 잘못된 타입(anon/secret) 또는 다른 프로젝트 ref의 키**를 사용해 `Invalid API key`가 나는 것이었고,
      코드의 결함이 아니라 **환경변수 조합 불일치** 문제였다.
- [14:39] 사용자 요청으로 운영/로컬 테스트 데이터 삭제 실행:
  - `assistant-test`, `vercel-prod-test`, `최종테스트`, `테스트 모임` 4건 제거.
  - 검증 결과 남은 행 0건.
  - 운영 API `https://paste-thumbs.vercel.app/api/meetings` 재확인 결과 `[]`.
- [14:57] [OAuth + 개인 공유 링크] 구현 시작:
  - OAuth 인증 기반 개인 사용자 식별 후, 사용자별 공유 URL 생성 기능 추가
  - 핵심 변경:
    - `next-auth` OAuth 라우팅(`src/app/api/auth/[...nextauth]/route.ts`) 추가
    - 세션 확장(`src/lib/auth.ts`, `src/types/next-auth.d.ts`) 및 `ownerShareToken` 생성 유틸
    - 공유 조회/권한 분기 적용:
      - `GET /api/meetings`는 로그인 사용자 or `ownerToken` 쿼리로 조회
      - `POST /api/meetings`는 로그인 사용자만 생성 가능
      - `PATCH /api/meetings/:id`는 소유자 세션 or 공유 토큰으로 수정 허용
    - UI 업데이트:
      - `GET /api/meetings` 결과를 사용자별로 조회/관리
      - 로그인/로그아웃 버튼 추가
      - `mode="shared"` 링크 페이지(`/share/[token]`) 생성: 개인 링크로 참여 가능한 뷰 추가
  - DB 마이그레이션 추가: `supabase/migrations/20260216000000_add_owner_share.sql`
    - `meetings.owner_email`, `meetings.owner_share_token` 컬럼 추가
  - 공지: 기존 DB에 컬럼 미반영 시 API가 `DB 스키마 업데이트가 필요합니다` 메시지 반환(현재 런타임 확인됨)
